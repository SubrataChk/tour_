import 'package:flutter/material.dart';

class LoginPageSection extends StatelessWidget {
  const LoginPageSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
