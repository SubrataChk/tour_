import 'package:flutter/material.dart';

class FavoritePageSection extends StatelessWidget {
  const FavoritePageSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Fav Page"),
      ),
    );
  }
}
